<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- <link rel="stylesheet" href="./output.css" /> -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title><%= pageTitle %></title>
  </head>

  <body class="font-[arial]">
    <!-- Navigation -->
    <%- include('../partials/nav') %>

    <main class="flex flex-col items-center mt-8">
      <!-- Page Heading -->
      <div
        class="w-[90%] h-24 flex justify-center items-center rounded-lg shadow-lg p-6 mb-6"
      >
        <span class="text-4xl font-semibold text-[#FF385C]">
          You are at Home Detail page
        </span>
      </div>

      <hr class="w-[90%] mb-6" />

      <!-- Home Detail Card -->
      <div class="w-[90%] mx-auto bg-white p-6 rounded-lg shadow-lg mt-6">
        <!-- Image Banner -->
        <div class="w-full h-100 rounded-lg overflow-hidden shadow-md">
          <img
            src="<%= home.imageUrl %>"
            alt="House Image"
            class="w-full h-full object-auto"
          />
        </div>

        <!-- Home Info -->
        <div class="mt-6">
          <h1 class="text-3xl font-bold text-[#FF385C] mb-3">
            <%= home.houseName %>
          </h1>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
            <p class="text-gray-700 text-lg">
              <strong>Location:</strong> <%= home.location %>
            </p>
            <p class="text-gray-700 text-lg">
              <strong>Price:</strong> Rs.<%= home.price %> / night
            </p>
            <p class="text-yellow-500 text-lg">
              <strong>Rating:</strong> ⭐ <%= home.rating %> / 5
            </p>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="mt-6 flex gap-4">
          <%- include('../partials/add-to-favourite', {home: home}) %>
          <button
            class="px-6 py-2 bg-blue-500 text-white text-base font-medium rounded-lg shadow hover:bg-blue-600 transition"
          >
            Book Now
          </button>
        </div>
      </div>
    </main>
  </body>
</html>
